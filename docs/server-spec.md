# Distribuidora MCP server — spec

TODO (Fase 3, blocked on catedrático approval of the use case): document each
tool once implemented — `consultar_inventario`, `estado_pedido`,
`estado_cuenta`, and later `crear_pedido` — with full `inputSchema`, error
codes, and real request/response examples.
