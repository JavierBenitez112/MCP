# Protocol notes

TODO (Fase 0): capture the real `initialize` / `initialized` handshake against
the official Filesystem and Git MCP servers using Claude Desktop + MCP
Inspector, per spec **2025-11-25** (not 2026-07-28 — that version drops the
handshake this project needs to analyze for requisito 7).

Record here: raw JSON-RPC for `initialize` request/response,
`notifications/initialized`, and one `tools/list` exchange per server.
